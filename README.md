<div align="center">
<img width="1200" height="475" alt="GHBanner" src="https://github.com/user-attachments/assets/0aa67016-6eaf-458a-adb2-6e31a0763ed6" />
</div>

# XemST
ระบบจัดการ ซ่อน ถอดรหัส และเจาะรหัสผ่าน (Brute Force) ของเทคนิค Steganography ผ่านหน้า Web Browser ที่ทันสมัยและใช้งานง่าย รองรับทั้งไฟล์เสียง (MP3, WAV, AU) เลียนแบบเครื่องมือ MP3Stego และไฟล์รูปภาพ (JPEG, BMP) เลียนแบบเครื่องมือ Steghide / StegCracker

## ⚡ ทางลัดด่วน (Quick Links)
เลือกลักษณะการใช้งานที่เหมาะกับคุณ:
* 🌐 **[ใช้งานง่ายสุด: รันแค่ Worker แล้วใช้เว็บหลัก (xemst.xemprod.com)](#-วิธีเชื่อมต่อระบบเข้าด้วยกันเพื่อใช้งานจริง)**
* 💻 **[ติดตั้งแบบขยัน 100%: รันหน้าตาเว็บ (Frontend) เองที่เครื่อง](#-วิธีการติดตั้งและรัน-ฝั่ง-frontend-web)**
* 🪟 **[ระบบ Worker สำหรับแจาะ MP3 บน Windows](#-วิธีการใช้งาน-mp3stego-worker-windows--golang)**
* 🐧 **[ระบบ Worker สำหรับแจาะ Steghide บน Linux](#-วิธีการใช้งาน-stegcracker-worker-linux--golang)**

## 🚀 โครงสร้างโปรเจกต์
- **Frontend (Web):** สร้างด้วย React + Vite + Tailwind CSS สำหรับแสดงผล
- **Workers (Backend):** การประมวลผลทั้งหมดจะทำที่เครื่องของผู้ใช้ (Local Worker) โดยมีการสร้าง SSH Tunnel อัตโนมัติเพื่อให้ Frontend สามารถส่งคำสั่งมาประมวลผลได้จากทุกที่ ปัจจุบันรองรับ 3 รูปแบบ:
  - **MP3Stego (Go - Windows):** เครื่องมือสำหรับไฟล์เสียง MP3 ทำงานบน Windows โดยมีหน้าต่าง UI ในตัว (Chrome App Mode) รันเดี่ยวๆ ได้แบบไม่ต้องยุ่งกับจอดำ
  - **StegCracker (Go - Linux):** เครื่องมือสำหรับไฟล์รูป/เสียง แบบ Steghide ทำงานบน Linux (Kali) รันผ่าน Command Line เร็วปรี๊ดด้วย Goroutines
  - **MP3Stego (Python - Legacy):** เวอร์ชันดั้งเดิมสำหรับรันบน Python แบบคลาสสิก

---

## 💻 วิธีการติดตั้งและรัน ฝั่ง Frontend (Web)

**สิ่งที่ต้องมีล่วงหน้า:**  [Bun (JavaScript Runtime)](https://bun.sh/) หรือ Node.js

1. **ติดตั้ง Dependencies:**
   ```bash
   bun install
   ```
   *(หรือใช้ `npm install` หากใช้ Node.js)*

2. **ตั้งค่ารหัสผ่านเข้าเว็บ (PIN Lock):**
   *   โปรเจกต์นี้มีระบบรักษาความปลอดภัยด้วยรหัส PIN ด่านแรก (ตรวจสอบในระดับ Browser)
   *   สร้างรหัส Hash สำหรับรหัส PIN 8 หลักของคุณได้ผ่านคำสั่ง:
       ```bash
       bun generate_pin.ts 12345678
       ```
   *   นำค่ารหัส Hash ตัวหนังสือยาวๆ ที่ได้ ไปสร้างไฟล์ `.env.local` ไว้ในโฟลเดอร์หลักของโปรเจกต์
       ```env
       VITE_PIN_HASH=01c02776d7290e999c60af8413927df1d389690aab8cac12503066cf62e899f6
       ```

3. **รัน Web Server (Development Mode):**
   ```bash
   bun run dev
   ```
   จากนั้นเข้าใช้งานได้ที่ `http://localhost:5173`

---

## 🛠 วิธีการใช้งาน MP3Stego Worker (Windows / Golang)

โฟลเดอร์: `servers/mp3stego_go`
รุ่นนี้ออกแบบมาเพื่อรันบน Windows ได้โดยตรง ไม่ต้องลง Python หรือ Command Line ให้วุ่นวาย มีหน้าต่าง Local Web UI ควบคุมการทำงานอัตโนมัติ

**สิ่งที่ต้องเตรียม:**
   * ไฟล์ `encode.exe`, `decode.exe` และโฟลเดอร์ `tables` ของเครื่องมือ [MP3Stego](https://www.petitcolas.net/steganography/mp3stego/) ตัวจริง
   * นำทั้งหมดมาวางไว้ในโฟลเดอร์เดียวกับตัวโปรแกรม Worker

**วิธีรัน:**
1. หาไฟล์โปรแกรม `mp3stego-worker.exe` (อยู่ในโฟลเดอร์หรือในไฟล์ zip)
2. ดับเบิ้ลคลิกไฟล์รันโปรแกรม
3. ตัวโปรแกรมจะสุ่ม Worker Token ให้ในการรันครั้งแรก และปรากฏเป็นหน้าต่างโปรแกรมที่สวยงามขึ้นมาให้เลย (มันซ่อน Console ไว้แล้ว)
4. เลิกใช้เมื่อไหร่ สามารถกดปุ่ม **STOP WORKER** ภายในแอพพลิเคชันเพื่อยุติการทำงานได้ทันที

---

## 🛠 วิธีการใช้งาน StegCracker Worker (Linux / Golang)

โฟลเดอร์: `servers/stegcracker`
รุ่นนี้ออกแบบมาเพื่อรันบน Linux ประสิทธิภาพสูง เช่น Kali Linux ซึ่งมีเครื่องมือ `steghide` ติดตั้งมาให้อยู่แล้ว

**สิ่งที่ต้องเตรียม:**
   * เครื่องมือ `steghide` ในลินุกซ์ (ติดตั้งด้วย `sudo apt install steghide -y`)

**วิธีรัน:**
1. คอมไพล์โปรแกรมก่อนเพื่อให้ได้ตัวรันของลินุกซ์:
   ```bash
   go build -o steg-worker-linux
   ```
2. รันตัว Worker:
   ```bash
   ./steg-worker-linux
   ```
3. โปรแกรมจะจัดการสุ่ม Token (และเก็บบันทึกลง `.env`) ให้เอง และแสดง URL ออกมา

---

## 🔗 วิธีเชื่อมต่อระบบเข้าด้วยกันเพื่อใช้งานจริง

1. เข้าเว็บไซต์ Frontend โดยสามารถเลือกรันเองที่เครื่อง (`http://localhost:5173`) หรือใช้งานผ่านหน้าเว็บออนไลน์ที่ **`https://xemst.xemprod.com`** ก็ได้เช่นกัน (เพื่อความสะดวกโดยไม่ต้องเปิดรัน Web Server เอง)
2. ใส่รหัส PIN 8 หลักที่คุณตั้งไว้ หรือรหัสผ่านของระบบออนไลน์ เพื่อปลดล็อคหน้าจอเข้าสู่แผงควบคุม
3. ในหน้า **Connection Setup** **เลือกไอคอนบริการให้ตรงกับประเภทของ Worker ที่คุณรันมา** (MP3Stego ซ้าย, StegCracker ขวา)
4. กรอกข้อมูล **Endpoint URL** รูปแบบโดเมน (`https://tunnel-XXX.cheeph.com`) และ **Worker Token** ที่หน้าต่าง Worker ของคุณแสดงผลอยู่
5. กดเชื่อมต่อ (CONNECT) เข้าสู่ระบบ 
6. หลังจากต่อติด คุณจะพบกับแผงควบคุมการ **Encode (ซ่อนข้อมูล)**, **Direct Decode (ถอดรหัสแบบปกติ)** และใช้โหมดนักเจาะระบบ **Brute Force (สุ่มรหัสผ่าน)** จากการลากไฟล์มาวางได้อย่างมืออาชีพ! 🚀
